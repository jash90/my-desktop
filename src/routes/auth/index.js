const login = require('./login')
const register = require('./register')
const resetPassword = require('./reset-password')
const forgotPassword = require('./reset-password')

module.exports = {
  login, register, resetPassword, forgotPassword
}
